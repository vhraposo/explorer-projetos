// 1. Declare uma variável de nome weight
var weight

// 2. Que tipo de dado é a variável acima ? 
/* undefined  -> */ console.log(typeof weight) 

/* 3. Declare uma variável e atribua valores para cada um dos dados : 
    * name: String
    *age: Number (integer)
    *stars: Number (float)
    *isSubscribed: Boolean
*/

        let person = {
            name: 'Victor',
            age: 23, 
            stars: 27.7,
            isSubscribed: true,
            
        }

        console.log(`O ${person.name} tem ${person.age} anos, ele tem ${person.stars} estrelas, mas ele está inscrito?${person.isSubscribed}`)





   
